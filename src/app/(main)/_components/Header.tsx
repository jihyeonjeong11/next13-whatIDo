const Header = () => {
  return (
    <header
      className={`fixed top-0 w-full h-14 border-b border-solid border-[#e5e7eb] z-40 bg-background`}
    >
      <p>header</p>
    </header>
  );
};

export default Header;
